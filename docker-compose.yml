services:
  retros:
    build: .
    ports:
      - "3333:3333"
    volumes:
      - retro-data:/app/data
    restart: unless-stopped
    environment:
      PORT: "3333"
      DB_PATH: "/app/data/retro.db"

volumes:
  retro-data:
